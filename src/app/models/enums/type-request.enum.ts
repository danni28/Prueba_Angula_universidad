export enum TypeRequest {
  POST,
  GET,
  DELETE,
  PUT,
}
export enum TypeResponse {
  ARRAY_BUFFER,
  BLOB,
  JSON,
  TEXT,
}
export enum Estado {
  EDITANDO = 'editando',
  CREANDO = 'creando',
}
